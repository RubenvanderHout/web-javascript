class Coordinates {

     /**
     * Constructs a new coordinates.
     * @param {string} x - X position
     * @param {number} y - Y position
     */
    constructor(x, y){
        this.x = x;
        this.y = y;
    }

    test() {
        console.log("todo")
    }
}